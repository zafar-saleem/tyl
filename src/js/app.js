/*global define:false */
define(function () {
	
	'use strict';
	
    var App = (function () {

        var init = function () {
            console.log('init');
        };

        return {
            init: init
        };

    }());

	return App;
});
